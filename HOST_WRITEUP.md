# 主辦方 write-up：CTF 農場

本遊戲僅供授權的校內資安體驗活動使用，請勿部署到公網。以下內容適合在挑戰結束後公布。

## 遊戲規則速覽

- 每位玩家以暱稱與六位數 PIN 建立帳號，瀏覽器取得一組個人 token。
- 玩家初始有 100 元、0 分，畫面上看得到 2×2 共四格田地（另有藏起來的 16 格，見「漏洞三」）。
- 五種種子的定價為 10、50、100、500、10,000 元。
- 作物成熟後可賣回種子原價的 1.5 倍，同時獲得 1、3、5、10、15 分。
- 種子庫存會顯示在獨立區塊；本局與累計排行榜各自依分數排序，只顯示前三名。
- 金錢與分數上限皆為 1,000,000。

## KOTH 回合規則

主辦方從 `/admin` 管理台開始活動。預設每局 10 分鐘、休息 1 分鐘，兩者都可以在管理台的「時間設定」隨時調整，改動會從下一局開始生效並存進資料庫。`.env` 裡的 `ROUND_SECONDS` 與 `BREAK_SECONDS` 只是初始值，在管理台改過之後就不再覆蓋。

- 「開始新活動（清除玩家）」會刪除所有玩家帳號、PIN、累計分數、登入 token 與種植資料，再開始第 1 局；管理員帳號、時間設定與公告會保留。
- 伺服器重開後若要延續既有玩家與累計分數，請使用「接續開始（保留累計）」；局數存在資料庫，所以它會接回原本的第 n 局，不會重新從第 1 局計數。
- 每局開始：所有玩家恢復為 100 元、0 本局分數，並清空種子、作物與全部 20 格田地。
- 每局進行中：本局排行榜顯示該局得分；累計排行榜顯示過去已結算分數加上目前得分。
- 每局結束：本局分數只結算一次並加入累計，休息期間仍顯示剛結束的本局成績。
- 休息結束：伺服器自動開始下一局。
- 主辦方可提早結算本局或提早開始下一局。
- 「暫停」會凍結倒數，進入既不算遊戲也不算休息的狀態。玩家的錢、種子、田地與本局分數都不動，但所有種田操作會被擋下；按「繼續」就從剩下的時間接回原本那一局。暫停**不刪除任何資料**。
- 非比賽時間會拒絕購買、種植、收成與販售 API。

| 種子 | 購買價 | 生長時間 | 售出金錢 | 售出分數 |
| --- | ---: | ---: | ---: | ---: |
| 番茄 | 10 元 | 0 分 10 秒 | 15 元 | 1 分 |
| 胡蘿蔔 | 50 元 | 0 分 30 秒 | 75 元 | 3 分 |
| 玉米 | 100 元 | 1 分 0 秒 | 150 元 | 5 分 |
| 南瓜 | 500 元 | 5 分 0 秒 | 750 元 | 10 分 |
| 黃金稻米 | 10,000 元 | 60 分 0 秒 | 15,000 元 | 15 分 |

## 建議的完整解題流程

1. 建立帳號並等待主辦方開始比賽回合。
2. 在商店購買黃金稻米，觀察新分頁網址中的 `totalprice`。
3. 把 `totalprice=10000` 改成 `totalprice=1` 後重新載入，以 1 元取得黃金稻米種子。
4. 回到遊戲頁面，確認「種子庫存」已同步，將黃金稻米種到任一格田地。
5. 在 DevTools 的 Elements 面板把該田地的 `data-planted-at` 改成 `0`。
6. 一秒內會出現「收成」按鈕。收成並賣出後可取得 15,000 元與 15 分。
7. 重複操作即可挑戰排行榜前三名。

## 漏洞一：信任 GET 參數中的購買總價

### 觀察方式

商店按下購買後，瀏覽器會開啟一個新分頁，網址格式如下：

```text
/buy_seed?token=<玩家 token>&seedtype=goldenRice&count=1&totalprice=10000
```

網址直接暴露四個參數：

- `token`：玩家身分。
- `seedtype`：種子種類。
- `count`：購買數量。
- `totalprice`：瀏覽器聲稱應扣除的總價。

### 觸發方式

把 `totalprice` 改成大於 0、但遠低於商品正價的數字，例如：

```text
/buy_seed?token=<玩家 token>&seedtype=goldenRice&count=1&totalprice=1
```

後端只檢查 `totalprice` 是有效數字且大於 0，沒有用 `seedtype` 和 `count` 重新計算價格，因此會用 1 元完成購買。`0`、負數及玩家目前無法支付的數字會被拒絕。

購買結果頁會通知原遊戲頁面同步金錢與種子庫存，不需要手動重新整理。

### 資安分類

- CWE-602：Client-Side Enforcement of Server-Side Security
- Parameter Tampering（參數竄改）
- 缺乏伺服器端價格驗證
- 使用 GET 執行會改變狀態的操作

### 正確修補方式

後端應只接受種子種類與數量，並以伺服器保存的商品價格自行計算總額。購買操作應改用 POST；但只把 GET 改成 POST 並不能解決信任用戶端價格的核心問題。

## 漏洞二：由瀏覽器判斷作物是否成熟

### 實作邏輯

按下「種下」時，伺服器會把當下的 Unix 時間戳寫入 SQLite。讀取玩家資料時，伺服器會將這個時間戳原樣傳給前端：

```html
<article data-plot-index="0"
         data-planted-at="1789061644248"
         data-grow-seconds="3600">
```

`data-planted-at` 是種植時間的 Unix 時間戳，單位為毫秒。前端每秒執行類似以下計算：

```js
const elapsed = Date.now() - Number(plotElement.dataset.plantedAt);
const mature = elapsed >= Number(plotElement.dataset.growSeconds) * 1000;
```

倒數更新只修改「剩餘 X 分 X 秒」文字、按鈕狀態與作物圖片的 `src`，**不會重建田地 DOM**，因此在 DevTools 修改屬性後不會被下一次畫面更新蓋掉。

作物圖也是照同一個被竄改過的時間算的，分三階段：前半段顯示種子、後半段顯示發芽、時間到顯示成熟的作物。所以把 `data-planted-at` 改成 `0` 之後，畫面會立刻跳成成熟的樣子。

### 觸發方式 A：修改 DOM 屬性

1. 種下一顆種子。
2. 按 F12 開啟 DevTools，進入 Elements 面板。
3. 找到對應田地的 `data-planted-at="..."`。
4. 將數值改成 `0`。
5. 等待最多一秒，該格田地就會顯示「可收成」。

### 觸發方式 B：修改前端變數

第一格田地的索引是 `0`，畫面上看得到的最後一格是 `3`（`4`～`19` 是藏起來的那 16 格）。在 Console 執行：

```js
player.plots[0].plantedAt = 0;
renderPlots();
```

### 觸發方式 C：改變本機時間或直接呼叫收成 URL

也可以把作業系統時間調到未來，讓 `Date.now()` 看起來已經超過成熟時間。

從 Network 或前端原始碼還能找到：

```text
/harvest?token=<玩家 token>&plot=0
```

直接在網址列開啟該 URL 也能收成。後端只確認該格有作物，不會重新計算是否真的成熟。直接使用 URL 後，回到遊戲頁重新載入即可看到更新後的狀態。

### 資安分類

- CWE-602：Client-Side Enforcement of Server-Side Security
- Client-Side Trust Issue
- 缺乏伺服器端業務邏輯驗證

### 正確修補方式

伺服器可以繼續保存可信任的種植時間，但每次收到收成請求時，都必須使用伺服器時間重新計算成熟狀態。前端倒數只能作為畫面提示，不能作為收成授權依據。

## 彩蛋：robots.txt 洩漏隱藏頁面

### 觀察方式

網站根目錄放了一個 `robots.txt`：

```text
User-agent: *
Disallow: /extrabigbonus
```

遊戲裡沒有任何連結指向 `/extrabigbonus`，只有翻過 `robots.txt` 的人才知道它存在。

### 觸發方式

直接在網址列開啟 `/extrabigbonus`。頁面上有一個 NCtfU logo，點一下加 1 分。身分沿用玩家在遊戲頁建立的同一組 token（同源，localStorage 直接共用），不需要重新登入；還沒建立帳號的人會被提示先回農場註冊。非比賽時間會擋下並說明現在點不計分（因為每局開始會重置本局分數）。

為了擋掉用腳本刷分，同一位玩家一秒內超過 30 次就會進入 30 秒冷卻（回 HTTP 429），頁面上會顯示倒數。人手最快大約 10 下／秒，正常點不會踩到這條線。這條路徑刻意不寫入 `activity.log`，免得把紀錄洗掉。

### 要講的重點

`robots.txt` 是「請搜尋引擎不要索引」的**約定**，不是存取控制：

- 它完全不會阻止任何人直接開啟那個網址。
- 把秘密路徑寫進去，等於主動把它公告出來——`robots.txt` 本來就是給所有人看的。
- 守規矩的爬蟲會遵守，惡意爬蟲反而會拿它當藏寶圖。

### 資安分類

- CWE-200：Exposure of Sensitive Information to an Unauthorized Actor
- Security through Obscurity（以隱藏代替防護）
- 誤把 robots.txt 當成存取控制

### 正確修補方式

不想讓人存取的路徑，要用真正的授權檢查（登入、權限判斷）擋住。若只是不希望被搜尋引擎收錄，應該用 `X-Robots-Tag` 標頭或頁面上的 `noindex`，並且路徑本身仍然要能擋掉未授權的訪客。

## 漏洞三：用 `hidden` 屬性把農地藏起來

### 實作邏輯

玩家其實有 **20 格**田地：畫面上的 2×2 共 4 格，以及一整區 4×4 共 16 格的「實驗農地」。後者在 HTML 裡長這樣：

```html
<section id="hidden-farm-panel" class="panel" hidden>
```

後端只用 `PLOT_COUNT`（= 20）檢查田地編號合不合法，**完全不在乎前端有沒有把那些格子畫出來**。前端 JS 也刻意從不去碰這個 `hidden` 屬性，所以刪掉之後不會被下一次重繪加回去。

### 觸發方式 A：在 Elements 面板刪掉屬性

1. 按 F12 開啟 DevTools，進入 Elements 面板。
2. 找到 `<section id="hidden-farm-panel" ... hidden>`。
3. 把 `hidden` 這個屬性刪掉。
4. 16 格實驗農地立刻出現，而且可以正常種、收成、賣出、計分。

### 觸發方式 B：完全不開畫面，直接打 API

田地編號 `4`～`19` 本來就會被接受，不需要先把區塊顯示出來：

```text
/plant?token=<玩家 token>&plot=19&seedtype=goldenRice
/harvest?token=<玩家 token>&plot=19
```

### 要講的重點

`hidden` 屬性（以及 `display: none`、`visibility: hidden`）只是**叫瀏覽器不要畫出來**，不是存取控制：

- HTML 原始碼本來就整份送到瀏覽器，藏起來的東西一樣在裡面。
- 使用者對自己瀏覽器裡的 DOM 有完全的控制權，想刪什麼屬性都可以。
- 真正決定「能不能做」的是伺服器。這裡伺服器從頭到尾都願意接受那 16 格，所以藏不藏其實沒有任何保護效果。

### 資安分類

- CWE-602：Client-Side Enforcement of Server-Side Security
- Hidden Form Fields / Hidden UI（以隱藏介面代替授權檢查）
- Security through Obscurity

### 正確修補方式

不該讓使用者用的功能，要在**伺服器端**擋掉，而不是在前端藏起來。以這個例子來說，伺服器應該只接受該玩家真正擁有的田地編號；前端要不要畫出來，只是顯示問題。

## 活動後檢視紀錄

伺服器會把操作附加寫入 `activity.log`，包含時間、API 名稱與請求參數。可用下列方式尋找解題痕跡：

```bash
grep 'buy_seed' activity.log
grep 'harvest' activity.log
```

講解時可以觀察：

- 黃金稻米的 `totalprice` 是否從 10,000 被改成 1。
- `plant` 後是否在遠短於正常生長時間的情況下出現 `harvest`。
- 同一位玩家是否連續組合兩個漏洞快速累積分數。

這份 log 只供活動後對照，不會即時封鎖、限流或阻止漏洞利用。
